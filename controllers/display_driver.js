index.update()

set_time.update()

update()
{
    
    if called from index current time is variable passed
    if called from set_time timer is reset to new time
    
    get_type will call twelve  or twenty_four functions which will format the outputs
}

get_type()

twelve_hour()

twenty_four_hour()